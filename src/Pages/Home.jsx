import React from 'react';
import { Box } from '@mui/material';

const Home = () => {
    return (
        <Box component='section'>
          home page  
        </Box>
    );
};

export default Home;