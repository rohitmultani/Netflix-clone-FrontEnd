import React from 'react';
import { Box } from '@mui/material';
const Login = () => {
    return (
        <Box variant="section">
            Login page component
        </Box>
    );
};

export default Login;