import React from 'react';
import { Box } from '@mui/material';

const Register = () => {
    return (
        <Box variant='section'>
            Register page
        </Box>
    );
};

export default Register;